# mcache

MCache is a local memory cache with some modern features, implemented LFU, LRU, FIFO, simple evication.

## examples

```bash
make build
cd build
cmake ..
make simple && ./simple
make cache && ./cache
```